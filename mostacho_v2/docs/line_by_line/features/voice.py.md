# Comentarios Línea por Línea: `features/voice.py`
Archivo fuente: `/Users/usuario/kenya/mostacho/src/mostacho/features/voice.py`

Formato: `L<numero> | codigo | explicacion tecnica`

- L1 | `"""Extraccion de features de voz para inferencia y entrenamiento."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L2 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L3 | `from __future__ import annotations` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L4 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L5 | `# logging permite reportar caidas de librerias opcionales.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L6 | `import logging` | Importa dependencias del módulo: logging.
- L7 | `# Path se usa para resolver rutas de audio de forma robusta.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L8 | `from pathlib import Path` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L9 | `# typing define contrato de salida.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L10 | `from typing import Dict` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L11 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L12 | `# numpy se usa para estadisticas basicas de señal.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L13 | `import numpy as np` | Importa dependencias del módulo: numpy as np.
- L14 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L15 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L16 | `# Logger del modulo para mensajes de depuracion.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L17 | `LOGGER = logging.getLogger(__name__)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L18 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L19 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L20 | `def extract_voice_features(audio_path: Path, target_sr: int = 16000) -> Dict[str, float]:` | Declara la función `extract_voice_features` con su firma de entrada/salida para encapsular lógica reutilizable.
- L21 | `    """Extrae features robustas desde un archivo de audio."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L22 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L23 | `    # Se normaliza entrada a Path por seguridad.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L24 | `    audio_path = Path(audio_path)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L25 | `    # Si el archivo no existe, se retorna vector neutro.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L26 | `    if not audio_path.exists():` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L27 | `        return {` | Retorna un valor al llamador como resultado explícito de la función.
- L28 | `            "voice_rms_mean": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L29 | `            "voice_rms_std": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L30 | `            "voice_zcr_mean": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L31 | `            "voice_spectral_centroid_mean": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L32 | `            "voice_duration_sec": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L33 | `        }` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L34 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L35 | `    # Se intenta usar librosa para extracción espectral rica.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L36 | `    try:` | Abre un bloque protegido para capturar excepciones durante operaciones potencialmente fallidas.
- L37 | `        # Import local para mantener carga perezosa.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L38 | `        import librosa  # type: ignore` | Importa dependencias del módulo: librosa  # type: ignore.
- L39 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L40 | `        # Se carga audio mono remuestreado a frecuencia objetivo.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L41 | `        signal, sample_rate = librosa.load(str(audio_path), sr=target_sr, mono=True)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L42 | `        # Si la señal viene vacía, se retorna vector neutro.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L43 | `        if signal.size == 0:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L44 | `            return {` | Retorna un valor al llamador como resultado explícito de la función.
- L45 | `                "voice_rms_mean": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L46 | `                "voice_rms_std": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L47 | `                "voice_zcr_mean": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L48 | `                "voice_spectral_centroid_mean": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L49 | `                "voice_duration_sec": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L50 | `            }` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L51 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L52 | `        # RMS por frame para energía de voz.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L53 | `        rms = librosa.feature.rms(y=signal)[0]` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L54 | `        # Tasa de cruces por cero para dinamica temporal.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L55 | `        zcr = librosa.feature.zero_crossing_rate(y=signal)[0]` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L56 | `        # Centroide espectral para balance frecuencial.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L57 | `        spectral_centroid = librosa.feature.spectral_centroid(y=signal, sr=sample_rate)[0]` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L58 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L59 | `        # Se entrega diccionario float listo para fusion.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L60 | `        return {` | Retorna un valor al llamador como resultado explícito de la función.
- L61 | `            "voice_rms_mean": float(np.mean(rms)),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L62 | `            "voice_rms_std": float(np.std(rms)),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L63 | `            "voice_zcr_mean": float(np.mean(zcr)),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L64 | `            "voice_spectral_centroid_mean": float(np.mean(spectral_centroid)),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L65 | `            "voice_duration_sec": float(signal.shape[0] / sample_rate),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L66 | `        }` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L67 | `    except Exception as exc:  # pragma: no cover - ruta de contingencia` | Captura una o más excepciones y define la estrategia de manejo de error.
- L68 | `        # Se registra warning y se aplica fallback sin librosa.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L69 | `        LOGGER.warning("Fallo extraccion avanzada de voz (%s). Se usa fallback simple.", exc)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L70 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L71 | `    # Fallback simple usando soundfile para minimizar dependencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L72 | `    try:` | Abre un bloque protegido para capturar excepciones durante operaciones potencialmente fallidas.
- L73 | `        # Import local para evitar fallo de modulo al importar paquete.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L74 | `        import soundfile as sf  # type: ignore` | Importa dependencias del módulo: soundfile as sf  # type: ignore.
- L75 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L76 | `        # Se lee señal completa.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L77 | `        signal, sample_rate = sf.read(str(audio_path), always_2d=False)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L78 | `        # Si el audio es estéreo, se promedia a mono.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L79 | `        if signal.ndim > 1:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L80 | `            signal = np.mean(signal, axis=1)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L81 | `        # Se convierte a float32 para cálculo estable.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L82 | `        signal = signal.astype(np.float32)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L83 | `        # Se calcula RMS global como energia media.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L84 | `        rms_value = float(np.sqrt(np.mean(np.square(signal)))) if signal.size else 0.0` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L85 | `        # Se calcula ZCR global aproximada.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L86 | `        zcr_value = float(np.mean(np.abs(np.diff(np.signbit(signal))))) if signal.size > 1 else 0.0` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L87 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L88 | `        # Se retorna vector reducido de contingencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L89 | `        return {` | Retorna un valor al llamador como resultado explícito de la función.
- L90 | `            "voice_rms_mean": rms_value,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L91 | `            "voice_rms_std": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L92 | `            "voice_zcr_mean": zcr_value,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L93 | `            "voice_spectral_centroid_mean": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L94 | `            "voice_duration_sec": float(signal.shape[0] / sample_rate) if sample_rate else 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L95 | `        }` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L96 | `    except Exception:` | Captura una o más excepciones y define la estrategia de manejo de error.
- L97 | `        # Si todo falla, se retorna neutro para no bloquear pipeline.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L98 | `        return {` | Retorna un valor al llamador como resultado explícito de la función.
- L99 | `            "voice_rms_mean": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L100 | `            "voice_rms_std": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L101 | `            "voice_zcr_mean": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L102 | `            "voice_spectral_centroid_mean": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L103 | `            "voice_duration_sec": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L104 | `        }` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
