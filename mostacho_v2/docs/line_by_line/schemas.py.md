# Comentarios Línea por Línea: `schemas.py`
Archivo fuente: `/Users/usuario/kenya/mostacho/src/mostacho/schemas.py`

Formato: `L<numero> | codigo | explicacion tecnica`

- L1 | `"""Esquemas Pydantic compartidos entre servicios y orquestador."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L2 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L3 | `from __future__ import annotations` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L4 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L5 | `# datetime permite sellar eventos con timestamp UTC.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L6 | `from datetime import datetime, timezone` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L7 | `# typing aporta tipos declarativos para APIs.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L8 | `from typing import Dict, List, Optional` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L9 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L10 | `# BaseModel valida y serializa datos de entrada/salida.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L11 | `from pydantic import BaseModel, Field` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L12 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L13 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L14 | `class FaceRequest(BaseModel):` | Declara la clase `FaceRequest` y su contrato de comportamiento dentro del módulo.
- L15 | `    """Entrada para el servicio de InsightFace."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L16 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L17 | `    # Se admite imagen en base64 para streaming desde cliente.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L18 | `    image_b64: Optional[str] = Field(default=None, description="Imagen codificada en base64.")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L19 | `    # Se admite ruta local para pruebas offline.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L20 | `    image_path: Optional[str] = Field(default=None, description="Ruta local de imagen para pruebas.")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L21 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L22 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L23 | `class FaceDetection(BaseModel):` | Declara la clase `FaceDetection` y su contrato de comportamiento dentro del módulo.
- L24 | `    """Representa un rostro detectado con atributos clave."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L25 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L26 | `    # Bounding box en formato [x1, y1, x2, y2].` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L27 | `    bbox: List[float]` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L28 | `    # Score de confianza del detector.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L29 | `    score: float` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L30 | `    # Landmarks 2D principales.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L31 | `    landmarks: List[List[float]]` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L32 | `    # Contorno de ojo izquierdo (6 puntos), cuando está disponible.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L33 | `    left_eye: List[List[float]] = Field(default_factory=list)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L34 | `    # Contorno de ojo derecho (6 puntos), cuando está disponible.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L35 | `    right_eye: List[List[float]] = Field(default_factory=list)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L36 | `    # EAR calculado para el rostro, cuando está disponible.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L37 | `    ear: Optional[float] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L38 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L39 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L40 | `class FaceResponse(BaseModel):` | Declara la clase `FaceResponse` y su contrato de comportamiento dentro del módulo.
- L41 | `    """Respuesta del servicio de vision."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L42 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L43 | `    # Timestamp UTC de procesamiento.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L44 | `    timestamp_utc: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L45 | `    # Lista de detecciones.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L46 | `    detections: List[FaceDetection]` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L47 | `    # Features resumidas para el modelo de fusion.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L48 | `    vision_features: Dict[str, float]` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L49 | `    # Estado de visión basado en EAR/somnolencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L50 | `    vision_state: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L51 | `    # EAR promedio suavizado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L52 | `    avg_ear: Optional[float] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L53 | `    # Duración continua de ojos cerrados.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L54 | `    closed_duration: float = Field(default=0.0)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L55 | `    # Umbral actual de EAR.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L56 | `    threshold: Optional[float] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L57 | `    # Baseline EAR calibrado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L58 | `    baseline: Optional[float] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L59 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L60 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L61 | `class SomnolenceControlResponse(BaseModel):` | Declara la clase `SomnolenceControlResponse` y su contrato de comportamiento dentro del módulo.
- L62 | `    """Respuesta para operaciones de control de calibración/umbral en visión."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L63 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L64 | `    # Estado de la operación solicitada.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L65 | `    status: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L66 | `    # Mensaje breve para consumo de cliente.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L67 | `    message: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L68 | `    # Umbral actual luego de una operación de ajuste (si aplica).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L69 | `    threshold: Optional[float] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L70 | `    # Timestamp UTC de la operación.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L71 | `    timestamp_utc: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L72 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L73 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L74 | `class FusionRequest(BaseModel):` | Declara la clase `FusionRequest` y su contrato de comportamiento dentro del módulo.
- L75 | `    """Entrada para el servicio de fusion TensorFlow."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L76 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L77 | `    # Features agregadas de vision.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L78 | `    vision_features: Dict[str, float] = Field(default_factory=dict)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L79 | `    # Features agregadas de voz.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L80 | `    voice_features: Dict[str, float] = Field(default_factory=dict)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L81 | `    # Features agregadas de biometria.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L82 | `    biometric_features: Dict[str, float] = Field(default_factory=dict)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L83 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L84 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L85 | `class FusionResponse(BaseModel):` | Declara la clase `FusionResponse` y su contrato de comportamiento dentro del módulo.
- L86 | `    """Salida de clasificacion multimodal."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L87 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L88 | `    # Estado final estimado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L89 | `    state: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L90 | `    # Confianza global entre 0 y 1.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L91 | `    confidence: float` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L92 | `    # Probabilidades por clase.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L93 | `    class_probabilities: Dict[str, float]` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L94 | `    # Timestamp UTC de inferencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L95 | `    timestamp_utc: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L96 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L97 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L98 | `class DrowsinessImageRequest(BaseModel):` | Declara la clase `DrowsinessImageRequest` y su contrato de comportamiento dentro del módulo.
- L99 | `    """Entrada para inferencia de somnolencia visual en TF service."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L100 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L101 | `    # Imagen codificada en base64 para envío por HTTP.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L102 | `    image_b64: Optional[str] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L103 | `    # Ruta local de imagen para pruebas offline.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L104 | `    image_path: Optional[str] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L105 | `    # Bounding box del rostro principal [x1, y1, x2, y2] (opcional).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L106 | `    face_bbox: Optional[List[float]] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L107 | `    # Estado visual proveniente de face_service (opcional).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L108 | `    vision_state: Optional[str] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L109 | `    # EAR promedio suavizado desde face_service (opcional).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L110 | `    avg_ear: Optional[float] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L111 | `    # Duración de ojos cerrados desde face_service (opcional).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L112 | `    closed_duration: float = Field(default=0.0)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L113 | `    # Umbral EAR utilizado en face_service (opcional).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L114 | `    threshold: Optional[float] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L115 | `    # Baseline EAR utilizado en face_service (opcional).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L116 | `    baseline: Optional[float] = Field(default=None)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L117 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L118 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L119 | `class DrowsinessResponse(BaseModel):` | Declara la clase `DrowsinessResponse` y su contrato de comportamiento dentro del módulo.
- L120 | `    """Salida de inferencia de somnolencia fusionando red visual + señales EAR."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L121 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L122 | `    # Estado final de somnolencia fusionado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L123 | `    state: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L124 | `    # Confianza del estado final entre 0 y 1.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L125 | `    confidence: float` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L126 | `    # Clase principal predicha por la red visual.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L127 | `    model_state: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L128 | `    # Confianza de la clase principal de la red visual.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L129 | `    model_confidence: float` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L130 | `    # Probabilidades por clase del modelo visual.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L131 | `    class_probabilities: Dict[str, float]` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L132 | `    # Puntaje de riesgo de somnolencia fusionado entre 0 y 1.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L133 | `    risk_score: float` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L134 | `    # Desglose de contribuciones del riesgo fusionado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L135 | `    components: Dict[str, float]` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L136 | `    # Timestamp UTC de inferencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L137 | `    timestamp_utc: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L138 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L139 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L140 | `class HealthResponse(BaseModel):` | Declara la clase `HealthResponse` y su contrato de comportamiento dentro del módulo.
- L141 | `    """Respuesta estandar de health-check."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L142 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L143 | `    # Nombre del servicio.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L144 | `    service: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L145 | `    # Estado operacional.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L146 | `    status: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L147 | `    # Hora UTC de respuesta.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L148 | `    timestamp_utc: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L149 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L150 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L151 | `def utc_now_iso() -> str:` | Declara la función `utc_now_iso` con su firma de entrada/salida para encapsular lógica reutilizable.
- L152 | `    """Retorna fecha/hora UTC en formato ISO 8601."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L153 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L154 | `    # Se normaliza a UTC para trazabilidad entre equipos y zonas horarias.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L155 | `    return datetime.now(tz=timezone.utc).isoformat()` | Retorna un valor al llamador como resultado explícito de la función.
