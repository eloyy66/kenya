# Comentarios Línea por Línea: `orchestrator/realtime_monitor.py`
Archivo fuente: `/Users/usuario/kenya/mostacho/src/mostacho/orchestrator/realtime_monitor.py`

Formato: `L<numero> | codigo | explicacion tecnica`

- L1 | `"""Monitor en tiempo real: combina face_service + tf_service para somnolencia."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L2 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L3 | `from __future__ import annotations` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L4 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L5 | `# argparse para configuración desde CLI.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L6 | `import argparse` | Importa dependencias del módulo: argparse.
- L7 | `# base64 para enviar frames por HTTP JSON.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L8 | `import base64` | Importa dependencias del módulo: base64.
- L9 | `# time para cálculo de FPS.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L10 | `import time` | Importa dependencias del módulo: time.
- L11 | `# typing para anotaciones explícitas.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L12 | `from typing import Any, Dict, List` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L13 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L14 | `# httpx para cliente HTTP síncrono.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L15 | `import httpx` | Importa dependencias del módulo: httpx.
- L16 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L17 | `# Configuración central de endpoints.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L18 | `from mostacho.settings import load_settings` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L19 | `# Wrapper de cámara compatible macOS/Windows/Linux.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L20 | `from mostacho.vision.camera import Camera, list_available_cameras` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L21 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L22 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L23 | `def _load_cv2() -> Any:` | Declara la función `_load_cv2` con su firma de entrada/salida para encapsular lógica reutilizable.
- L24 | `    """Carga OpenCV de forma diferida."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L25 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L26 | `    # Import local para evitar side-effects en import del módulo.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L27 | `    import cv2  # type: ignore` | Importa dependencias del módulo: cv2  # type: ignore.
- L28 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L29 | `    # Retorna módulo cv2 listo para usar.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L30 | `    return cv2` | Retorna un valor al llamador como resultado explícito de la función.
- L31 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L32 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L33 | `def _encode_frame_to_b64(frame: Any) -> str:` | Declara la función `_encode_frame_to_b64` con su firma de entrada/salida para encapsular lógica reutilizable.
- L34 | `    """Codifica un frame BGR como JPEG base64."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L35 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L36 | `    # Carga OpenCV para compresión JPEG.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L37 | `    cv2 = _load_cv2()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L38 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L39 | `    # Codifica imagen con calidad alta para conservar rasgos faciales.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L40 | `    ok, buffer = cv2.imencode(".jpg", frame, [int(cv2.IMWRITE_JPEG_QUALITY), 90])` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L41 | `    if not ok:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L42 | `        raise RuntimeError("No se pudo codificar frame a JPEG.")` | Lanza una excepción para propagar un error o violación de invariantes.
- L43 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L44 | `    # Convierte bytes JPEG a cadena base64.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L45 | `    return base64.b64encode(buffer.tobytes()).decode("utf-8")` | Retorna un valor al llamador como resultado explícito de la función.
- L46 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L47 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L48 | `def _draw_overlay(frame: Any, face_payload: Dict[str, Any], drowsiness_payload: Dict[str, Any], fps: float) -> Any:` | Declara la función `_draw_overlay` con su firma de entrada/salida para encapsular lógica reutilizable.
- L49 | `    """Dibuja overlays de detección y estado final en el frame."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L50 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L51 | `    # Carga OpenCV para dibujo.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L52 | `    cv2 = _load_cv2()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L53 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L54 | `    # Copia de frame para no alterar buffer original.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L55 | `    output = frame.copy()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L56 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L57 | `    # Detecciones faciales retornadas por face_service.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L58 | `    detections: List[Dict[str, Any]] = list(face_payload.get("detections", []))` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L59 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L60 | `    # Dibuja bbox y contorno de ojos por detección.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L61 | `    for detection in detections:` | Inicia un bucle `for` para iterar secuencialmente sobre una colección o generador.
- L62 | `        bbox = detection.get("bbox") or []` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L63 | `        if len(bbox) == 4:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L64 | `            x1, y1, x2, y2 = [int(value) for value in bbox]` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L65 | `            cv2.rectangle(output, (x1, y1), (x2, y2), (0, 255, 0), 2)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L66 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L67 | `        for x, y in detection.get("left_eye", []):` | Inicia un bucle `for` para iterar secuencialmente sobre una colección o generador.
- L68 | `            cv2.circle(output, (int(x), int(y)), 2, (0, 255, 255), -1)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L69 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L70 | `        for x, y in detection.get("right_eye", []):` | Inicia un bucle `for` para iterar secuencialmente sobre una colección o generador.
- L71 | `            cv2.circle(output, (int(x), int(y)), 2, (0, 255, 255), -1)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L72 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L73 | `    # Campos principales de salida fusionada.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L74 | `    fused_state = str(drowsiness_payload.get("state", "UNKNOWN"))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L75 | `    fused_conf = float(drowsiness_payload.get("confidence", 0.0))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L76 | `    model_state = str(drowsiness_payload.get("model_state", "unknown"))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L77 | `    model_conf = float(drowsiness_payload.get("model_confidence", 0.0))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L78 | `    risk_score = float(drowsiness_payload.get("risk_score", 0.0))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L79 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L80 | `    # Estado de visión proveniente de EAR/landmarks.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L81 | `    vision_state = str(face_payload.get("vision_state", "NO_FACE"))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L82 | `    avg_ear = face_payload.get("avg_ear")` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L83 | `    closed_duration = float(face_payload.get("closed_duration", 0.0) or 0.0)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L84 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L85 | `    # Color por severidad del estado final.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L86 | `    color = (0, 255, 0)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L87 | `    if fused_state == "DROWSY_WARNING":` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L88 | `        color = (0, 165, 255)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L89 | `    elif fused_state == "SOMNOLENT":` | Define una condición alternativa dentro de la misma cadena condicional.
- L90 | `        color = (0, 0, 255)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L91 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L92 | `    # Texto de estado final fusionado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L93 | `    cv2.putText(output, f"FUSED: {fused_state} ({fused_conf:.2f})", (10, 30), cv2.FONT_HERSHEY_SIMPLEX, 0.9, color, 2)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L94 | `    # Texto de clase del modelo visual.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L95 | `    cv2.putText(output, f"MODEL: {model_state} ({model_conf:.2f})", (10, 60), cv2.FONT_HERSHEY_SIMPLEX, 0.7, (255, 255, 255), 2)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L96 | `    # Texto de estado visual por EAR.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L97 | `    cv2.putText(output, f"VISION: {vision_state}", (10, 90), cv2.FONT_HERSHEY_SIMPLEX, 0.7, (180, 255, 180), 2)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L98 | `    # Texto de EAR.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L99 | `    if avg_ear is not None:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L100 | `        cv2.putText(output, f"EAR: {float(avg_ear):.3f}", (10, 120), cv2.FONT_HERSHEY_SIMPLEX, 0.7, (255, 255, 255), 2)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L101 | `    # Texto de duración de ojos cerrados.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L102 | `    cv2.putText(output, f"Closed: {closed_duration:.2f}s", (10, 150), cv2.FONT_HERSHEY_SIMPLEX, 0.7, (255, 255, 255), 2)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L103 | `    # Texto de riesgo fusionado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L104 | `    cv2.putText(output, f"Risk: {risk_score:.2f}", (10, 180), cv2.FONT_HERSHEY_SIMPLEX, 0.7, (255, 200, 200), 2)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L105 | `    # Texto de FPS.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L106 | `    cv2.putText(output, f"FPS: {fps:.1f}", (10, 210), cv2.FONT_HERSHEY_SIMPLEX, 0.7, (255, 255, 0), 2)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L107 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L108 | `    # Retorna frame anotado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L109 | `    return output` | Retorna un valor al llamador como resultado explícito de la función.
- L110 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L111 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L112 | `def parse_args() -> argparse.Namespace:` | Declara la función `parse_args` con su firma de entrada/salida para encapsular lógica reutilizable.
- L113 | `    """Parsea argumentos CLI para monitor en tiempo real."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L114 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L115 | `    # Configuración básica del parser.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L116 | `    parser = argparse.ArgumentParser(description="Realtime monitor (face_service + tf_service)")` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L117 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L118 | `    # Índice de cámara local.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L119 | `    parser.add_argument("--camera-index", type=int, default=0, help="Indice de camara")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L120 | `    # Backend de cámara recomendado AUTO para multiplataforma.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L121 | `    parser.add_argument("--camera-backend", type=str, default="AUTO", help="AUTO|AVFOUNDATION|DSHOW|MSMF|V4L2")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L122 | `    # Resolución de captura.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L123 | `    parser.add_argument("--width", type=int, default=640, help="Ancho de captura")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L124 | `    parser.add_argument("--height", type=int, default=360, help="Alto de captura")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L125 | `    # Lista cámaras detectadas y termina.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L126 | `    parser.add_argument("--list-cameras", action="store_true", help="Lista cámaras disponibles y sale")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L127 | `    # Índice máximo a probar en descubrimiento.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L128 | `    parser.add_argument("--max-camera-index", type=int, default=10, help="Indice maximo a probar al listar cámaras")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L129 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L130 | `    # Endpoint opcional de face_service.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L131 | `    parser.add_argument("--face-url", type=str, default=None, help="URL base de face_service")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L132 | `    # Endpoint opcional de tf_service.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L133 | `    parser.add_argument("--tf-url", type=str, default=None, help="URL base de tf_service")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L134 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L135 | `    # Timeout de requests HTTP.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L136 | `    parser.add_argument("--timeout", type=float, default=15.0, help="Timeout HTTP en segundos")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L137 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L138 | `    # Retorna argumentos parseados.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L139 | `    return parser.parse_args()` | Retorna un valor al llamador como resultado explícito de la función.
- L140 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L141 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L142 | `def main() -> None:` | Declara la función `main` con su firma de entrada/salida para encapsular lógica reutilizable.
- L143 | `    """Ejecuta monitor de somnolencia en tiempo real."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L144 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L145 | `    # Argumentos del usuario.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L146 | `    args = parse_args()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L147 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L148 | `    # Modo descubrimiento de cámaras sin levantar monitor.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L149 | `    if args.list_cameras:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L150 | `        cameras = list_available_cameras(` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L151 | `            max_index=args.max_camera_index,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L152 | `            backend=args.camera_backend,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L153 | `            width=args.width,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L154 | `            height=args.height,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L155 | `        )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L156 | `        if not cameras:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L157 | `            print("No se detectaron camaras en el rango solicitado.")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L158 | `            return` | Finaliza la función y retorna `None` de forma explícita.
- L159 | `        print("Camaras detectadas:")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L160 | `        for camera_index, backend_name in cameras:` | Inicia un bucle `for` para iterar secuencialmente sobre una colección o generador.
- L161 | `            print(f"  index={camera_index} backend={backend_name}")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L162 | `        return` | Finaliza la función y retorna `None` de forma explícita.
- L163 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L164 | `    # Carga configuración global de endpoints.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L165 | `    settings = load_settings()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L166 | `    face_url = args.face_url or settings.face_service_url` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L167 | `    tf_url = args.tf_url or settings.tf_service_url` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L168 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L169 | `    # Inicializa cámara local.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L170 | `    camera = Camera(index=args.camera_index, backend=args.camera_backend, width=args.width, height=args.height)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L171 | `    if not camera.open():` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L172 | `        raise RuntimeError("No se pudo abrir la camara local.")` | Lanza una excepción para propagar un error o violación de invariantes.
- L173 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L174 | `    # Calentamiento para estabilizar exposición/foco.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L175 | `    camera.warmup(frames=10)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L176 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L177 | `    # Carga OpenCV para ventana de visualización.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L178 | `    cv2 = _load_cv2()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L179 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L180 | `    # Cliente HTTP para llamadas a servicios locales.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L181 | `    timeout = httpx.Timeout(args.timeout)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L182 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L183 | `    # Buffers de última respuesta para tolerar errores transitorios de red.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L184 | `    last_face_payload: Dict[str, Any] = {"detections": [], "vision_state": "NO_FACE", "closed_duration": 0.0}` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L185 | `    last_drowsiness_payload: Dict[str, Any] = {` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L186 | `        "state": "UNKNOWN",` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L187 | `        "confidence": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L188 | `        "model_state": "unknown",` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L189 | `        "model_confidence": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L190 | `        "risk_score": 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L191 | `    }` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L192 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L193 | `    # Variables para medición de FPS.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L194 | `    previous_time = 0.0` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L195 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L196 | `    print("Monitor en vivo iniciado. Tecla 'q' para salir.")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L197 | `    print(f"face_service: {face_url}")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L198 | `    print(f"tf_service:   {tf_url}")` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L199 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L200 | `    # Bucle principal de captura e inferencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L201 | `    with httpx.Client(timeout=timeout) as client:` | Abre un contexto administrado para garantizar adquisición/liberación segura de recursos.
- L202 | `        while True:` | Inicia un bucle `while` controlado por una condición de repetición.
- L203 | `            # Captura frame local.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L204 | `            ok, frame = camera.read()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L205 | `            if not ok or frame is None:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L206 | `                break` | Interrumpe de inmediato el bucle actual.
- L207 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L208 | `            # Codifica frame a base64 para ambas APIs.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L209 | `            frame_b64 = _encode_frame_to_b64(frame)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L210 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L211 | `            # Llama a face_service para detección + EAR.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L212 | `            try:` | Abre un bloque protegido para capturar excepciones durante operaciones potencialmente fallidas.
- L213 | `                face_response = client.post(f"{face_url}/infer", json={"image_b64": frame_b64})` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L214 | `                face_response.raise_for_status()` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L215 | `                face_payload = face_response.json()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L216 | `                last_face_payload = face_payload` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L217 | `            except Exception:` | Captura una o más excepciones y define la estrategia de manejo de error.
- L218 | `                # Reusa última respuesta válida si falla request.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L219 | `                face_payload = last_face_payload` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L220 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L221 | `            # Toma bbox del rostro principal si existe.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L222 | `            detections = face_payload.get("detections", []) or []` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L223 | `            primary_bbox = detections[0].get("bbox") if detections else None` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L224 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L225 | `            # Payload para tf_service con salida de face_service.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L226 | `            drowsiness_request = {` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L227 | `                "image_b64": frame_b64,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L228 | `                "face_bbox": primary_bbox,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L229 | `                "vision_state": face_payload.get("vision_state"),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L230 | `                "avg_ear": face_payload.get("avg_ear"),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L231 | `                "closed_duration": face_payload.get("closed_duration", 0.0),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L232 | `                "threshold": face_payload.get("threshold"),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L233 | `                "baseline": face_payload.get("baseline"),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L234 | `            }` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L235 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L236 | `            # Llama a tf_service para estado de somnolencia fusionado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L237 | `            try:` | Abre un bloque protegido para capturar excepciones durante operaciones potencialmente fallidas.
- L238 | `                tf_response = client.post(f"{tf_url}/predict/drowsiness_image", json=drowsiness_request)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L239 | `                tf_response.raise_for_status()` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L240 | `                drowsiness_payload = tf_response.json()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L241 | `                last_drowsiness_payload = drowsiness_payload` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L242 | `            except Exception:` | Captura una o más excepciones y define la estrategia de manejo de error.
- L243 | `                # Reusa última salida válida si falla request.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L244 | `                drowsiness_payload = last_drowsiness_payload` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L245 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L246 | `            # Cálculo de FPS instantáneo.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L247 | `            current_time = time.time()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L248 | `            fps = 1.0 / (current_time - previous_time) if previous_time > 0 else 0.0` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L249 | `            previous_time = current_time` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L250 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L251 | `            # Dibuja overlays con resultados actuales.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L252 | `            output = _draw_overlay(frame, face_payload, drowsiness_payload, fps)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L253 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L254 | `            # Muestra ventana de monitor.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L255 | `            cv2.imshow("Mostacho Drowsiness Monitor", output)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L256 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L257 | `            # Salida por teclado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L258 | `            key = cv2.waitKey(1) & 0xFF` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L259 | `            if key == ord("q"):` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L260 | `                break` | Interrumpe de inmediato el bucle actual.
- L261 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L262 | `    # Libera cámara y recursos gráficos.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L263 | `    camera.release()` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L264 | `    cv2.destroyAllWindows()` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L265 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L266 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L267 | `if __name__ == "__main__":` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L268 | `    # Entrada directa del script.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L269 | `    main()` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
