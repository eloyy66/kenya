# Comentarios Línea por Línea: `services/face_api.py`
Archivo fuente: `/Users/usuario/kenya/mostacho/src/mostacho/services/face_api.py`

Formato: `L<numero> | codigo | explicacion tecnica`

- L1 | `"""Servicio FastAPI para visión base: InsightFace + EAR + somnolencia."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L2 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L3 | `from __future__ import annotations` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L4 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L5 | `# base64 para transportar imágenes por HTTP.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L6 | `import base64` | Importa dependencias del módulo: base64.
- L7 | `# logging para trazabilidad de errores y estado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L8 | `import logging` | Importa dependencias del módulo: logging.
- L9 | `# Path para manejo robusto de rutas de archivo.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L10 | `from pathlib import Path` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L11 | `# time para usar reloj monotónico en inferencia secuencial.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L12 | `import time` | Importa dependencias del módulo: time.
- L13 | `# typing para anotaciones explícitas.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L14 | `from typing import Any, List` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L15 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L16 | `# numpy para cálculo de métricas agregadas.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L17 | `import numpy as np` | Importa dependencias del módulo: numpy as np.
- L18 | `# FastAPI para exponer endpoints de inferencia/control.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L19 | `from fastapi import FastAPI, HTTPException, Query` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L20 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L21 | `# Esquemas compartidos de entrada/salida.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L22 | `from mostacho.schemas import (` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L23 | `    FaceDetection,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L24 | `    FaceRequest,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L25 | `    FaceResponse,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L26 | `    HealthResponse,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L27 | `    SomnolenceControlResponse,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L28 | `    utc_now_iso,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L29 | `)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L30 | `# Runtime visual que porta la base de madebycodex.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L31 | `from mostacho.vision.runtime import VisionRuntime, VisionRuntimeConfig` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L32 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L33 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L34 | `# Logger de módulo para observabilidad en producción/desarrollo.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L35 | `LOGGER = logging.getLogger(__name__)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L36 | `# App FastAPI principal.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L37 | `app = FastAPI(title="Mostacho Face Service", version="0.2.0")` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L38 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L39 | `# Cache global del objeto FaceAnalysis.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L40 | `_FACE_APP: Any = None` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L41 | `# Cache global del runtime stateful de somnolencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L42 | `_VISION_RUNTIME: VisionRuntime | None = None` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L43 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L44 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L45 | `def _load_cv2() -> Any:` | Declara la función `_load_cv2` con su firma de entrada/salida para encapsular lógica reutilizable.
- L46 | `    """Importa OpenCV de forma diferida para evitar side-effects globales."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L47 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L48 | `    # Import local para minimizar fallas al importar módulo completo.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L49 | `    import cv2  # type: ignore` | Importa dependencias del módulo: cv2  # type: ignore.
- L50 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L51 | `    # Retorna módulo cv2 listo para uso.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L52 | `    return cv2` | Retorna un valor al llamador como resultado explícito de la función.
- L53 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L54 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L55 | `def _load_insightface_app() -> Any:` | Declara la función `_load_insightface_app` con su firma de entrada/salida para encapsular lógica reutilizable.
- L56 | `    """Inicializa FaceAnalysis una sola vez y lo deja en cache."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L57 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L58 | `    # Se usa variable global para singleton.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L59 | `    global _FACE_APP` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L60 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L61 | `    # Si ya existe instancia, se reutiliza.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L62 | `    if _FACE_APP is not None:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L63 | `        return _FACE_APP` | Retorna un valor al llamador como resultado explícito de la función.
- L64 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L65 | `    # Import local para aislar dependencia de insightface al servicio de visión.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L66 | `    from insightface.app import FaceAnalysis  # type: ignore` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L67 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L68 | `    # Configura modelo y módulos de landmarks requeridos.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L69 | `    face_app = FaceAnalysis(` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L70 | `        name="buffalo_l",` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L71 | `        providers=["CPUExecutionProvider"],` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L72 | `        allowed_modules=["detection", "landmark_3d_68", "landmark_2d_106"],` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L73 | `    )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L74 | `    # Inicialización en CPU con tamaño de detección estable.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L75 | `    face_app.prepare(ctx_id=-1, det_size=(320, 320))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L76 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L77 | `    # Cachea instancia para futuras requests.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L78 | `    _FACE_APP = face_app` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L79 | `    return _FACE_APP` | Retorna un valor al llamador como resultado explícito de la función.
- L80 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L81 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L82 | `def _load_runtime() -> VisionRuntime:` | Declara la función `_load_runtime` con su firma de entrada/salida para encapsular lógica reutilizable.
- L83 | `    """Inicializa runtime de visión stateful y lo deja en cache."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L84 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L85 | `    # Se usa variable global para singleton runtime.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L86 | `    global _VISION_RUNTIME` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L87 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L88 | `    # Si ya existe runtime, se devuelve de inmediato.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L89 | `    if _VISION_RUNTIME is not None:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L90 | `        return _VISION_RUNTIME` | Retorna un valor al llamador como resultado explícito de la función.
- L91 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L92 | `    # Se asegura inicialización del detector base.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L93 | `    face_app = _load_insightface_app()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L94 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L95 | `    # Config por defecto equivalente a base de madebycodex.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L96 | `    config = VisionRuntimeConfig(` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L97 | `        detect_size=(320, 320),` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L98 | `        window_size=5,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L99 | `        closed_seconds=3.0,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L100 | `        calibration_seconds=2.0,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L101 | `        threshold_offset=0.04,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L102 | `        min_threshold=0.15,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L103 | `    )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L104 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L105 | `    # Se crea runtime y se cachea.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L106 | `    _VISION_RUNTIME = VisionRuntime(face_app=face_app, config=config)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L107 | `    return _VISION_RUNTIME` | Retorna un valor al llamador como resultado explícito de la función.
- L108 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L109 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L110 | `def _decode_image_from_request(payload: FaceRequest) -> np.ndarray:` | Declara la función `_decode_image_from_request` con su firma de entrada/salida para encapsular lógica reutilizable.
- L111 | `    """Decodifica imagen desde base64 o ruta local."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L112 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L113 | `    # Se carga OpenCV bajo demanda.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L114 | `    cv2 = _load_cv2()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L115 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L116 | `    # Ruta de decodificación por base64.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L117 | `    if payload.image_b64:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L118 | `        # Decodifica texto base64 a bytes binarios.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L119 | `        raw_bytes = base64.b64decode(payload.image_b64)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L120 | `        # Interpreta bytes como arreglo uint8.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L121 | `        image_array = np.frombuffer(raw_bytes, dtype=np.uint8)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L122 | `        # Decodifica buffer a imagen BGR.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L123 | `        image = cv2.imdecode(image_array, cv2.IMREAD_COLOR)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L124 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L125 | `        # Valida decodificación correcta.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L126 | `        if image is None:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L127 | `            raise HTTPException(status_code=400, detail="No se pudo decodificar image_b64.")` | Lanza una excepción para propagar un error o violación de invariantes.
- L128 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L129 | `        # Retorna imagen lista para inferencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L130 | `        return image` | Retorna un valor al llamador como resultado explícito de la función.
- L131 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L132 | `    # Ruta de lectura por archivo local.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L133 | `    if payload.image_path:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L134 | `        # Normaliza path.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L135 | `        image_path = Path(payload.image_path)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L136 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L137 | `        # Verifica existencia de archivo.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L138 | `        if not image_path.exists():` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L139 | `            raise HTTPException(status_code=400, detail=f"No existe la ruta: {image_path}")` | Lanza una excepción para propagar un error o violación de invariantes.
- L140 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L141 | `        # Lee archivo de imagen con OpenCV.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L142 | `        image = cv2.imread(str(image_path))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L143 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L144 | `        # Valida lectura correcta.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L145 | `        if image is None:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L146 | `            raise HTTPException(status_code=400, detail=f"No se pudo leer imagen: {image_path}")` | Lanza una excepción para propagar un error o violación de invariantes.
- L147 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L148 | `        # Retorna imagen BGR.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L149 | `        return image` | Retorna un valor al llamador como resultado explícito de la función.
- L150 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L151 | `    # Si no llega ningún input de imagen, se rechaza request.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L152 | `    raise HTTPException(status_code=400, detail="Debe enviar image_b64 o image_path.")` | Lanza una excepción para propagar un error o violación de invariantes.
- L153 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L154 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L155 | `def _vision_features_from_response(detections: List[FaceDetection], image_shape: tuple[int, ...], response: FaceResponse) -> dict[str, float]:` | Declara la función `_vision_features_from_response` con su firma de entrada/salida para encapsular lógica reutilizable.
- L156 | `    """Construye features de visión para fusión multimodal."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L157 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L158 | `    # Extrae dimensiones para normalizar área.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L159 | `    height, width = image_shape[:2]` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L160 | `    frame_area = float(height * width) if height and width else 1.0` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L161 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L162 | `    # Métricas base de detección facial.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L163 | `    face_count = float(len(detections))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L164 | `    avg_score = 0.0` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L165 | `    primary_face_area_ratio = 0.0` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L166 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L167 | `    # Cálculos cuando hay al menos un rostro.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L168 | `    if detections:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L169 | `        # Promedio de score de detección.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L170 | `        avg_score = float(np.mean([det.score for det in detections]))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L171 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L172 | `        # Área del rostro principal (primer rostro).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L173 | `        x1, y1, x2, y2 = detections[0].bbox` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L174 | `        face_area = max(0.0, (x2 - x1)) * max(0.0, (y2 - y1))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L175 | `        primary_face_area_ratio = float(face_area / frame_area)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L176 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L177 | `    # Flags de estado visual para facilitar consumo por tf_service.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L178 | `    state = response.vision_state` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L179 | `    is_calibrating = 1.0 if state == "CALIBRATING" else 0.0` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L180 | `    is_attentive = 1.0 if state == "ATTENTIVE" else 0.0` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L181 | `    is_eyes_closed = 1.0 if state == "EYES_CLOSED" else 0.0` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L182 | `    is_somnolent = 1.0 if state == "SOMNOLENT" else 0.0` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L183 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L184 | `    # Vector final de features visuales.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L185 | `    features = {` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L186 | `        "vision_face_count": face_count,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L187 | `        "vision_avg_score": avg_score,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L188 | `        "vision_primary_face_area_ratio": primary_face_area_ratio,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L189 | `        "vision_avg_ear": float(response.avg_ear) if response.avg_ear is not None else 0.0,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L190 | `        "vision_closed_duration": float(response.closed_duration),` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L191 | `        "vision_is_calibrating": is_calibrating,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L192 | `        "vision_is_attentive": is_attentive,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L193 | `        "vision_is_eyes_closed": is_eyes_closed,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L194 | `        "vision_is_somnolent": is_somnolent,` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L195 | `    }` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L196 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L197 | `    # Añade umbral actual si está disponible.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L198 | `    if response.threshold is not None:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L199 | `        features["vision_threshold"] = float(response.threshold)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L200 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L201 | `    # Añade baseline si está disponible.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L202 | `    if response.baseline is not None:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L203 | `        features["vision_baseline"] = float(response.baseline)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L204 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L205 | `    # Retorna vector de features listo para fusión.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L206 | `    return features` | Retorna un valor al llamador como resultado explícito de la función.
- L207 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L208 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L209 | `@app.get("/health", response_model=HealthResponse)` | Aplica un decorador para modificar el comportamiento de la función/clase declarada a continuación.
- L210 | `def health() -> HealthResponse:` | Declara la función `health` con su firma de entrada/salida para encapsular lógica reutilizable.
- L211 | `    """Health check liviano del servicio de visión."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L212 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L213 | `    # Respuesta estándar de disponibilidad.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L214 | `    return HealthResponse(service="face_service", status="ok", timestamp_utc=utc_now_iso())` | Retorna un valor al llamador como resultado explícito de la función.
- L215 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L216 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L217 | `@app.post("/infer", response_model=FaceResponse)` | Aplica un decorador para modificar el comportamiento de la función/clase declarada a continuación.
- L218 | `def infer(payload: FaceRequest) -> FaceResponse:` | Declara la función `infer` con su firma de entrada/salida para encapsular lógica reutilizable.
- L219 | `    """Ejecuta detección facial + EAR/somnolencia en una imagen."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L220 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L221 | `    # Se carga runtime stateful de visión.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L222 | `    runtime = _load_runtime()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L223 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L224 | `    # Se decodifica imagen de entrada.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L225 | `    image = _decode_image_from_request(payload)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L226 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L227 | `    # Análisis visual completo (detección + EAR + estado).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L228 | `    analysis = runtime.analyze_image(image, now=time.monotonic())` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L229 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L230 | `    # Serializa detecciones al esquema público API.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L231 | `    detections: List[FaceDetection] = []` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L232 | `    for item in analysis.detections:` | Inicia un bucle `for` para iterar secuencialmente sobre una colección o generador.
- L233 | `        detections.append(` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L234 | `            FaceDetection(` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L235 | `                bbox=item.bbox,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L236 | `                score=item.score,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L237 | `                landmarks=item.landmarks,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L238 | `                left_eye=item.left_eye,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L239 | `                right_eye=item.right_eye,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L240 | `                ear=item.ear,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L241 | `            )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L242 | `        )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L243 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L244 | `    # Construye respuesta base sin features (se rellenan después).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L245 | `    response = FaceResponse(` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L246 | `        timestamp_utc=utc_now_iso(),` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L247 | `        detections=detections,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L248 | `        vision_features={},` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L249 | `        vision_state=analysis.vision_state,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L250 | `        avg_ear=analysis.avg_ear,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L251 | `        closed_duration=analysis.closed_duration,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L252 | `        threshold=analysis.threshold,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L253 | `        baseline=analysis.baseline,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L254 | `    )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L255 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L256 | `    # Construye features visuales desde el análisis completo.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L257 | `    response.vision_features = _vision_features_from_response(detections, image.shape, response)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L258 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L259 | `    # Retorna respuesta final.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L260 | `    return response` | Retorna un valor al llamador como resultado explícito de la función.
- L261 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L262 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L263 | `@app.post("/somnolence/reset", response_model=SomnolenceControlResponse)` | Aplica un decorador para modificar el comportamiento de la función/clase declarada a continuación.
- L264 | `def reset_somnolence() -> SomnolenceControlResponse:` | Declara la función `reset_somnolence` con su firma de entrada/salida para encapsular lógica reutilizable.
- L265 | `    """Reinicia calibración y estado de somnolencia del runtime visual."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L266 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L267 | `    # Se obtiene runtime global.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L268 | `    runtime = _load_runtime()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L269 | `    # Se reinicia calibración y baseline.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L270 | `    runtime.reset_calibration()` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L271 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L272 | `    # Respuesta de confirmación.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L273 | `    return SomnolenceControlResponse(` | Retorna un valor al llamador como resultado explícito de la función.
- L274 | `        status="ok",` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L275 | `        message="Calibracion de somnolencia reiniciada.",` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L276 | `        threshold=None,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L277 | `        timestamp_utc=utc_now_iso(),` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L278 | `    )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L279 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L280 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L281 | `@app.post("/somnolence/threshold", response_model=SomnolenceControlResponse)` | Aplica un decorador para modificar el comportamiento de la función/clase declarada a continuación.
- L282 | `def adjust_somnolence_threshold(delta: float = Query(..., ge=-0.1, le=0.1)) -> SomnolenceControlResponse:` | Declara la función `adjust_somnolence_threshold` con su firma de entrada/salida para encapsular lógica reutilizable.
- L283 | `    """Ajusta umbral manual de EAR con delta positivo o negativo."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L284 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L285 | `    # Se obtiene runtime global.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L286 | `    runtime = _load_runtime()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L287 | `    # Se aplica ajuste incremental y se recupera nuevo umbral.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L288 | `    threshold = runtime.adjust_threshold(delta)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L289 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L290 | `    # Respuesta con umbral actualizado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L291 | `    return SomnolenceControlResponse(` | Retorna un valor al llamador como resultado explícito de la función.
- L292 | `        status="ok",` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L293 | `        message="Umbral de somnolencia actualizado.",` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L294 | `        threshold=threshold,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L295 | `        timestamp_utc=utc_now_iso(),` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L296 | `    )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
