# Comentarios Línea por Línea: `features/biometrics.py`
Archivo fuente: `/Users/usuario/kenya/mostacho/src/mostacho/features/biometrics.py`

Formato: `L<numero> | codigo | explicacion tecnica`

- L1 | `"""Utilidades de extracción de features biométricas desde CSV/TXT."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L2 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L3 | `from __future__ import annotations` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L4 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L5 | `# Path para manejo de rutas portable.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L6 | `from pathlib import Path` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L7 | `# typing para contratos de salida.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L8 | `from typing import Dict` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L9 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L10 | `# numpy para estadistica numerica.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L11 | `import numpy as np` | Importa dependencias del módulo: numpy as np.
- L12 | `# pandas para carga tabular flexible.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L13 | `import pandas as pd` | Importa dependencias del módulo: pandas as pd.
- L14 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L15 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L16 | `def _read_table(path: Path) -> pd.DataFrame:` | Declara la función `_read_table` con su firma de entrada/salida para encapsular lógica reutilizable.
- L17 | `    """Carga tabla con separador inferido y fallback de texto plano."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L18 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L19 | `    # Primer intento: CSV estándar con coma.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L20 | `    try:` | Abre un bloque protegido para capturar excepciones durante operaciones potencialmente fallidas.
- L21 | `        return pd.read_csv(path)` | Retorna un valor al llamador como resultado explícito de la función.
- L22 | `    except Exception:` | Captura una o más excepciones y define la estrategia de manejo de error.
- L23 | `        pass` | Marcador nulo: mantiene la estructura sintáctica mientras no hay acción concreta.
- L24 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L25 | `    # Segundo intento: delimitador por espacios o tabuladores.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L26 | `    try:` | Abre un bloque protegido para capturar excepciones durante operaciones potencialmente fallidas.
- L27 | `        return pd.read_csv(path, sep=r"\s+", engine="python")` | Retorna un valor al llamador como resultado explícito de la función.
- L28 | `    except Exception:` | Captura una o más excepciones y define la estrategia de manejo de error.
- L29 | `        pass` | Marcador nulo: mantiene la estructura sintáctica mientras no hay acción concreta.
- L30 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L31 | `    # Tercer intento: archivo de una columna numérica.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L32 | `    data = np.loadtxt(path, dtype=float)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L33 | `    # Si el array es 1D, se convierte a DataFrame con nombre base.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L34 | `    if data.ndim == 1:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L35 | `        return pd.DataFrame({"value": data})` | Retorna un valor al llamador como resultado explícito de la función.
- L36 | `    # Si es 2D, se crean columnas automáticas.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L37 | `    return pd.DataFrame(data)` | Retorna un valor al llamador como resultado explícito de la función.
- L38 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L39 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L40 | `def extract_biometrics_features(path: Path, max_columns: int = 8) -> Dict[str, float]:` | Declara la función `extract_biometrics_features` con su firma de entrada/salida para encapsular lógica reutilizable.
- L41 | `    """Extrae estadisticas resumidas para fusion multimodal."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L42 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L43 | `    # Se normaliza entrada de ruta.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L44 | `    path = Path(path)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L45 | `    # Si no existe archivo, se retorna vector neutro.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L46 | `    if not path.exists():` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L47 | `        return {"bio_available": 0.0}` | Retorna un valor al llamador como resultado explícito de la función.
- L48 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L49 | `    # Se carga la tabla de señales.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L50 | `    table = _read_table(path)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L51 | `    # Se seleccionan columnas numéricas únicamente.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L52 | `    numeric = table.select_dtypes(include=[np.number])` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L53 | `    # Si no hay columnas numéricas, se retorna indicador mínimo.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L54 | `    if numeric.empty:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L55 | `        return {"bio_available": 0.0}` | Retorna un valor al llamador como resultado explícito de la función.
- L56 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L57 | `    # Se limita cantidad de columnas para mantener vector manejable.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L58 | `    selected_cols = list(numeric.columns[:max_columns])` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L59 | `    # Se prepara diccionario de salida.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L60 | `    features: Dict[str, float] = {"bio_available": 1.0, "bio_num_rows": float(len(numeric))}` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L61 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L62 | `    # Se agregan estadisticas por columna seleccionada.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L63 | `    for column in selected_cols:` | Inicia un bucle `for` para iterar secuencialmente sobre una colección o generador.
- L64 | `        # Serie limpia sin NaNs para estadística robusta.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L65 | `        values = pd.to_numeric(numeric[column], errors="coerce").dropna().to_numpy(dtype=float)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L66 | `        # Si no hay datos válidos, se continúa.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L67 | `        if values.size == 0:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L68 | `            continue` | Salta al siguiente ciclo del bucle actual sin ejecutar el resto del cuerpo.
- L69 | `        # Prefijo normalizado por nombre de columna.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L70 | `        key = f"bio_{str(column).lower()}"` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L71 | `        # Se calculan métricas de primer orden.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L72 | `        features[f"{key}_mean"] = float(np.mean(values))` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L73 | `        features[f"{key}_std"] = float(np.std(values))` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L74 | `        features[f"{key}_min"] = float(np.min(values))` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L75 | `        features[f"{key}_max"] = float(np.max(values))` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L76 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L77 | `    # Se entrega vector final.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L78 | `    return features` | Retorna un valor al llamador como resultado explícito de la función.
