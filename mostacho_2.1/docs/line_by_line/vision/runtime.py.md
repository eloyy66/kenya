# Comentarios Línea por Línea: `vision/runtime.py`
Archivo fuente: `/Users/usuario/kenya/mostacho/src/mostacho/vision/runtime.py`

Formato: `L<numero> | codigo | explicacion tecnica`

- L1 | `"""Runtime de visión que integra InsightFace + landmarks + somnolencia."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L2 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L3 | `from __future__ import annotations` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L4 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L5 | `# dataclass para estructuras de salida claras.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L6 | `from dataclasses import dataclass, field` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L7 | `# time para reloj monotónico en inferencia por frame.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L8 | `import time` | Importa dependencias del módulo: time.
- L9 | `# typing para firmas explícitas.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L10 | `from typing import Any, List, Tuple` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L11 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L12 | `# numpy para vectores numéricos.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L13 | `import numpy as np` | Importa dependencias del módulo: numpy as np.
- L14 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L15 | `# utilidades de ojos y detector de somnolencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L16 | `from mostacho.vision.eyes import compute_ear, get_eye_landmarks_from_face` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L17 | `from mostacho.vision.somnolence import SomnolenceDetector` | Importa símbolos específicos desde otro módulo para reducir referencias calificadas en el código.
- L18 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L19 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L20 | `@dataclass` | Aplica un decorador para modificar el comportamiento de la función/clase declarada a continuación.
- L21 | `class VisionRuntimeConfig:` | Declara la clase `VisionRuntimeConfig` y su contrato de comportamiento dentro del módulo.
- L22 | `    """Configuración de runtime visual (detección + EAR)."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L23 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L24 | `    # Tamaño de detección para InsightFace.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L25 | `    detect_size: Tuple[int, int] = (320, 320)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L26 | `    # Tamaño de ventana para suavizado EAR.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L27 | `    window_size: int = 5` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L28 | `    # Segundos de ojos cerrados para declarar somnolencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L29 | `    closed_seconds: float = 3.0` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L30 | `    # Segundos iniciales para calibrar baseline.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L31 | `    calibration_seconds: float = 2.0` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L32 | `    # Offset para umbral dinámico.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L33 | `    threshold_offset: float = 0.04` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L34 | `    # Umbral mínimo de seguridad.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L35 | `    min_threshold: float = 0.15` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L36 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L37 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L38 | `@dataclass` | Aplica un decorador para modificar el comportamiento de la función/clase declarada a continuación.
- L39 | `class AnalyzedFace:` | Declara la clase `AnalyzedFace` y su contrato de comportamiento dentro del módulo.
- L40 | `    """Estructura serializable de un rostro analizado."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L41 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L42 | `    # Bounding box [x1, y1, x2, y2].` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L43 | `    bbox: List[float]` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L44 | `    # Score de detección.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L45 | `    score: float` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L46 | `    # Landmarks faciales principales.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L47 | `    landmarks: List[List[float]] = field(default_factory=list)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L48 | `    # Contorno de ojo izquierdo (6 puntos).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L49 | `    left_eye: List[List[float]] = field(default_factory=list)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L50 | `    # Contorno de ojo derecho (6 puntos).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L51 | `    right_eye: List[List[float]] = field(default_factory=list)` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L52 | `    # EAR calculado para el rostro principal.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L53 | `    ear: float | None = None` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L54 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L55 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L56 | `@dataclass` | Aplica un decorador para modificar el comportamiento de la función/clase declarada a continuación.
- L57 | `class VisionAnalysis:` | Declara la clase `VisionAnalysis` y su contrato de comportamiento dentro del módulo.
- L58 | `    """Resultado de análisis de visión por frame."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L59 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L60 | `    # Lista de rostros detectados/anotados.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L61 | `    detections: List[AnalyzedFace]` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L62 | `    # Estado visual final.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L63 | `    vision_state: str` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L64 | `    # EAR promedio suavizado actual.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L65 | `    avg_ear: float | None` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L66 | `    # Duración continua de ojos cerrados.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L67 | `    closed_duration: float` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L68 | `    # Umbral actual usado por el detector.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L69 | `    threshold: float | None` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L70 | `    # Baseline EAR calibrado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L71 | `    baseline: float | None` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L72 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L73 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L74 | `class VisionRuntime:` | Declara la clase `VisionRuntime` y su contrato de comportamiento dentro del módulo.
- L75 | `    """Motor reutilizable de visión para servicio HTTP y ejecución local."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L76 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L77 | `    def __init__(self, face_app: Any, config: VisionRuntimeConfig | None = None):` | Declara la función `__init__` con su firma de entrada/salida para encapsular lógica reutilizable.
- L78 | `        # Instancia FaceAnalysis ya inicializada.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L79 | `        self.face_app = face_app` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L80 | `        # Configuración del runtime visual.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L81 | `        self.config = config or VisionRuntimeConfig()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L82 | `        # Detector stateful de somnolencia por EAR.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L83 | `        self.somnolence = SomnolenceDetector(` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L84 | `            window_size=self.config.window_size,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L85 | `            closed_seconds=self.config.closed_seconds,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L86 | `            calibration_seconds=self.config.calibration_seconds,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L87 | `            threshold_offset=self.config.threshold_offset,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L88 | `            min_threshold=self.config.min_threshold,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L89 | `        )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L90 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L91 | `    def reset_calibration(self) -> None:` | Declara la función `reset_calibration` con su firma de entrada/salida para encapsular lógica reutilizable.
- L92 | `        """Reinicia calibración EAR interna."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L93 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L94 | `        # Delegación al detector de somnolencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L95 | `        self.somnolence.reset_calibration()` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L96 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L97 | `    def adjust_threshold(self, delta: float) -> float:` | Declara la función `adjust_threshold` con su firma de entrada/salida para encapsular lógica reutilizable.
- L98 | `        """Ajusta umbral manual de EAR y retorna valor actualizado."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L99 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L100 | `        # Delegación de ajuste al detector stateful.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L101 | `        return self.somnolence.adjust_threshold(delta)` | Retorna un valor al llamador como resultado explícito de la función.
- L102 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L103 | `    def analyze_image(self, image: np.ndarray, now: float | None = None) -> VisionAnalysis:` | Declara la función `analyze_image` con su firma de entrada/salida para encapsular lógica reutilizable.
- L104 | `        """Analiza una imagen/frame y devuelve detecciones + estado visual."""` | Docstring de una sola línea que documenta el bloque inmediatamente asociado.
- L105 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L106 | `        # Usa reloj monotónico cuando no se provee timestamp.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L107 | `        if now is None:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L108 | `            now = time.monotonic()` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L109 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L110 | `        # Detección facial vía InsightFace.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L111 | `        faces = self.face_app.get(image)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L112 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L113 | `        # Serialización de detecciones para salida.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L114 | `        serialized: List[AnalyzedFace] = []` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L115 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L116 | `        # Valores por defecto cuando no hay rostro/landmarks.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L117 | `        vision_state = "NO_FACE"` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L118 | `        avg_ear: float | None = None` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L119 | `        closed_duration = 0.0` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L120 | `        threshold: float | None = None` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L121 | `        baseline: float | None = self.somnolence.baseline_ear` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L122 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L123 | `        # Se procesa cada rostro detectado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L124 | `        for index, face in enumerate(faces):` | Inicia un bucle `for` para iterar secuencialmente sobre una colección o generador.
- L125 | `            # Bounding box normalizado a lista float.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L126 | `            bbox = [float(value) for value in face.bbox.tolist()]` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L127 | `            # Score de detección normalizado.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L128 | `            score = float(getattr(face, "det_score", 0.0))` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L129 | `            # Landmarks básicos (kps) para visualización general.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L130 | `            kps = getattr(face, "kps", None)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L131 | `            landmarks = [[float(x), float(y)] for x, y in kps.tolist()] if kps is not None else []` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L132 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L133 | `            # Extracción de contorno de ojos (6 puntos por ojo).` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L134 | `            left_eye, right_eye = get_eye_landmarks_from_face(face)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L135 | `            # Valores default de ojos en lista vacía.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L136 | `            left_eye_list: List[List[float]] = []` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L137 | `            right_eye_list: List[List[float]] = []` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L138 | `            ear_value: float | None = None` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L139 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L140 | `            # Si hay landmarks válidos de ojos, calcula EAR.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L141 | `            if left_eye is not None and right_eye is not None:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L142 | `                # Convertir puntos a listas serializables.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L143 | `                left_eye_list = [[float(x), float(y)] for x, y in left_eye.tolist()]` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L144 | `                right_eye_list = [[float(x), float(y)] for x, y in right_eye.tolist()]` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L145 | `                # EAR promedio de ambos ojos.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L146 | `                ear_value = float((compute_ear(left_eye) + compute_ear(right_eye)) / 2.0)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L147 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L148 | `                # Solo el rostro principal actualiza estado de somnolencia.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L149 | `                if index == 0:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L150 | `                    somnolence = self.somnolence.update(ear_value, now=now)` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L151 | `                    vision_state = str(somnolence["state"])` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L152 | `                    avg_ear = float(somnolence["avg_ear"]) if somnolence["avg_ear"] is not None else None` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L153 | `                    closed_duration = float(somnolence["closed_duration"]) if somnolence["closed_duration"] is not None else 0.0` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L154 | `                    threshold = float(somnolence["threshold"]) if somnolence["threshold"] is not None else None` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L155 | `                    baseline = float(somnolence["baseline"]) if somnolence["baseline"] is not None else None` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L156 | `            else:` | Define la rama por defecto cuando ninguna condición previa se cumple.
- L157 | `                # Si no hay ojos válidos en el rostro principal, estado específico.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L158 | `                if index == 0:` | Evalúa una condición booleana y abre una rama de ejecución condicional.
- L159 | `                    vision_state = "NO_LANDMARKS"` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L160 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L161 | `            # Se agrega detección analizada a salida.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L162 | `            serialized.append(` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L163 | `                AnalyzedFace(` | Ejecuta una instrucción de la lógica del módulo (transformación, control de flujo o coordinación de datos).
- L164 | `                    bbox=bbox,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L165 | `                    score=score,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L166 | `                    landmarks=landmarks,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L167 | `                    left_eye=left_eye_list,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L168 | `                    right_eye=right_eye_list,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L169 | `                    ear=ear_value,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L170 | `                )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L171 | `            )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
- L172 | `<línea en blanco>` | Línea en blanco usada para separar bloques lógicos y mejorar la legibilidad.
- L173 | `        # Se retorna estructura de análisis completa.` | Comentario del autor que aclara intención, decisión técnica o contexto operativo.
- L174 | `        return VisionAnalysis(` | Retorna un valor al llamador como resultado explícito de la función.
- L175 | `            detections=serialized,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L176 | `            vision_state=vision_state,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L177 | `            avg_ear=avg_ear,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L178 | `            closed_duration=closed_duration,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L179 | `            threshold=threshold,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L180 | `            baseline=baseline,` | Realiza una asignación para persistir un valor intermedio o configuración de ejecución.
- L181 | `        )` | Ejecuta una llamada de función/método para producir un efecto o calcular un resultado.
